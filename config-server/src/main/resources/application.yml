server:
  port: 8888   # Config Server will run on this port

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/itsawanish/Centralized-microservice-config-server.git
          default-label: main
          clone-on-start: true
